[tool.ruff]
target-version = "py313"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pyright]
pythonVersion = "3.13"
typeCheckingMode = "basic"
reportDeprecated = "error"
include = ["app", "scripts", "tests"]
exclude = [".venv"]
venvPath = "."
venv = ".venv"
